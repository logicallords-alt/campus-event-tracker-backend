{
  "name": "backend",
  "version": "1.0.0",
  "description": "Campus Event Tracker Backend - Express.js API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-frontend": "cd ../frontend && npm install && npm run build && cd ../backend && mkdir -p dist && rm -rf dist/* && cp -r ../frontend/dist/* dist/ && echo '✅ Frontend built and copied to backend/dist'",
    "prebuild-frontend": "echo 'Building frontend...'",
    "postbuild-frontend": "ls -la dist/ | head -5 && echo 'Build complete!'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.3.2",
    "multer": "^2.1.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "engines": {
    "node": "18.x || 20.x"
  }
}
